<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dude.karteikarten">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".AddCardActivity" />
        <activity
            android:name=".Edit2Activity"
            android:label="@string/title_activity_edit2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LessonEditActivity" />
        <activity android:name=".CardObjectActivity" />
        <activity android:name=".DeckEditActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".LearnActivity" />
        <activity android:name=".CardsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/card_flip_right_out" />
    </application>

</manifest>